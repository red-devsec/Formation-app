name: CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: formations_db
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: admin123
        ports:
          - 5432:5432
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t formation-app .

      - name: Run PostgreSQL test
        run: |
          docker run --rm --network host postgres:16-alpine \
            psql -h localhost -U admin -d formations_db -c "SELECT 1"

      - name: Run container
        run: docker run -d -p 5000:5000 --name app formation-app

      - name: Test application
        run: |
          sleep 5
          curl --fail http://localhost:5000/
          curl --fail http://localhost:5000/formations

      - name: Cleanup
        run: |
          docker stop app
          docker rm app